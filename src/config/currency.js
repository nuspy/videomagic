export const SUPPORTED_CURRENCIES = ['EUR', 'USD']
export const DEFAULT_CURRENCY = 'EUR'

// Simple VAT mapping: EU VAT on EUR, none on USD (placeholder)
export const VAT_RATES = {
  EUR: 0.22,
  USD: 0.0,
}

// Stripe mapping placeholder (same codes here)
export const STRIPE_CURRENCY_MAP = {
  EUR: 'eur',
  USD: 'usd',
}
