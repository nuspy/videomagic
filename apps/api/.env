# Server
NODE_ENV=development
PORT=4000
# Allow common localhost dev origins (Vite and Nginx static in docker)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080
# Cookies (httpOnly session JWT)
COOKIE_SAMESITE=Lax
COOKIE_NAME_ACCESS=access_token
COOKIE_NAME_REFRESH=refresh_token

# Database (MariaDB)
# Localhost dev (adjust user/pass if needed)
DATABASE_URL="mysql://root:root@localhost:3306/app_db"

# Google OAuth (set in Google Cloud Console)
GOOGLE_CLIENT_ID=832520801523-gmbfrveerb54fvjphvsqteo4hvi2v4du.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
OAUTH_REDIRECT_URI=http://localhost:4000/auth/google/callback

# JWT (set strong secrets in real env)
JWT_ACCESS_SECRET=dev-access-secret
JWT_REFRESH_SECRET=dev-refresh-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# SMTP (Mailpit dev)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Starter App <no-reply@example.com>"

# S3 / MinIO (dev defaults)
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET_UPLOADS=uploads
S3_FORCE_PATH_STYLE=true
S3_SIGNED_URL_EXPIRES=900

# Upload limits
MAX_UPLOAD_BYTES=10485760 # 10MB

# Payments - Stripe (test)
PAYMENTS_DEFAULT_CURRENCY=eur
VAT_FALLBACK_RATE=0.22
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Payments - PayPal (sandbox)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_WEBHOOK_ID=
PAYPAL_ENV=sandbox

# Wavespeed WAN 2.2
WAVESPEED_API_BASE=
WAVESPEED_API_KEY=
